# UI Spec: signpost-checklist-redesign

**N/A.** This feature is a Stop-hook mechanism (session-first-turn verification check) with no
screens, user flows, or UI surface of any kind. Per `02-ARCHITECTURE.md`, the only interfaces are
the hook's stdin/stdout JSON contract and its Signpost/Pillar text convention within an agent's
own reply — neither is a UI. No `@ui-spec-writer` dispatch was made; this file exists so the
standard spec sequence has a verifiable artifact at this path, not because UI design work
occurred.
