# Repository and service register

Statuses and maturity terms follow the charter. Documentation claims are not treated as operational proof.

| Observed name | Type / location | Apparent purpose and entry point | Dependencies / consumers | Evidence | Status | Maturity | Open questions |
|---|---|---|---|---|---|---|---|
| Agent Rig repository | Git repository, `/home/d-tuned/agent-rig` | Source workshop for prompt bundles, hooks, probes, templates, and audit artifacts; no standalone application entry point | Git, Claude Code runtime | `README.md:1-28`; committed tree: 408 files at HEAD | Implemented | Operational but fragile | Which distributed surfaces are authoritative when installed copies drift? |
| Spec Orchestration 07152026 | Prompt bundle, `spec-orchestration-07152026/` | `/spec-start`; Intake → Interview → documents → Frank gate → human approval | 6 agent definitions, 6 skills, 8 templates, global Claude install | `commands/spec-start.md:13-63`, `:201-218`, `:495-527`; installer `install.sh:13-88`; installed-copy comparison: 19/21 matching | Present — unverified | Operational but fragile | Two installed templates differ; no runtime workflow test exists |
| Forge 07152026 | Prompt bundle, `forge-07152026/` | `/forge-start`; delegates code/test/QC/Git roles, then Frank forge gate | 7 agents, 7 skills, 4 templates, Git/GitHub/project test stack | `commands/forge-start.md:13-31`, `:193-274`, `:336-471`; installer `install.sh:16-95`; all package comparisons matched | Present — unverified | Operational but fragile | Command behavior conflicts with README on governance-file handling |
| Notebook Orchestration 04182026 | Prompt bundle, `notebook-orchestration-04182026/` | `/notebook-start`; design → build → Jupyter execution → mechanical QC → Frank | 5 agents, 4 skills, 4 templates, Jupyter/nbconvert | `commands/notebook-start.md:40-120`; `skills/notebook-execute/SKILL.md:29-49`; global assets mostly match, but Frank collides; `jupyter` absent | Partial | Prototype | Is this host expected to execute notebooks, or only distribute the bundle? |
| Lit Synthesis 04292026 | Prompt bundle, `lit-synthesis-04292026/` | `/lit-synthesis-init`, `/run`, `/curate`; file-based corpus pipeline | 5 agents, 5 skills, 10 templates, PDFs and project-local governance/artifacts | command headers and sequences; installer `install.sh:13-109`; all compared global assets match | Present — unverified | Operational but fragile | External PDF/tool requirements and real runs were not verified |
| Frank | Cross-cutting agent persona, `agents/frank.md` | Read-only judgment gate producing PASS/FAIL/HALT | Claude agent runtime; spec/forge/notebook commands | metadata/tools `agents/frank.md:1-15`; verdict contract `:76-100`; installed `~/.claude/agents/frank.md` differs from HEAD | Partial | Operational but fragile | Which Frank definition actually governs runtime? Notebook package also owns a `frank.md` destination |
| `/new-project` | Global command source, `commands/new-project.md` | 13-step bootstrap including files, GitHub, LORE, relay, commit, and push | `gh`, Git, templates, LORE gateway, Switchboard | `commands/new-project.md:5-31`, `:360-461`, `:499-679`, `:813-855`; installed copy matches | Present — unverified | Operational but fragile | No non-mutating end-to-end verification path |
| `/lore-close` | Global command source, `commands/lore-close.md` | Reconciles state and writes a closing LORE capture; may push after a gate | LORE gateway, Git/upstream, local instructions | `commands/lore-close.md:6-23`, `:52-98`, `:111-137`; installed copy differs | Partial | Operational but fragile | Which copy is source of truth? |
| `/relay` | Repo-local command, `.claude/commands/relay.md` | Read/respond to Switchboard messages | `read_messages`, `send_message` app tools | `.claude/commands/relay.md:1-7` | Present — unverified | Prototype | External relay state was excluded |
| Session queue hook | Project-local SessionStart hook, `.claude/hooks/session-queue.sh` + `scripts/session_queue_probe.py` | Reads newest tagged, nonsuperseded LORE queue and injects it as unverified context | ignored `CLAUDE.md` project ID; `~/runtime/agent-lore/.env`; psycopg2; Postgres; local transcripts | route `.claude/settings.json:2-12`; read-only SQL `scripts/session_queue_probe.py:66-149`; injection/staleness `:267-407` | Present — unverified | Operational but fragile | LORE/database compatibility and a current successful fire were not tested |
| Progress proof hook | Project-local PreToolUse hook/probe | Intercepts `Edit` of `*PROGRESS.md`; optionally executes embedded allowlisted `PROOF:` commands; blocks failures/ambiguity | project allowlist, shell, ignored JSONL | route `.claude/settings.json:14-23`; decision and execution `scripts/progress_proof_per_slice_probe.py:120-254`, `:524-594`; 705 local log rows; wrapper tests 9/15 | Partial | Operational but fragile | Is optional/manual-unverified proof intended? Is the Write bypass acceptable? |
| No-preamble reminder/analyzer | UserPromptSubmit reminder plus Stop hook | Injects style reminder; analyzes assistant response | local/global Claude settings; ignored JSONL | `.claude/settings.json:25-46`; reminder `scripts/no_preamble_reminder.py:17-42`; `MODE = "log_only"` at `scripts/no_preamble_probe.py:49-51`; 1,172 log rows | Implemented as advisory; blocking enforcement Partial | Operational | Whether global + project registration double-fires is indeterminate |
| Signpost checklist hook | Stop hook/probe | On first turn after a successful queue marker, requires Signpost/Pillar rows and checks cited tool IDs exist | transcript JSONL, session queue marker, local/global settings, ignored JSONL | gating `scripts/signpost_checklist_probe.py:405-480`, `:828-894`; evaluation `:536-654`; 238 log rows; Python tests passed | Implemented | Operational but fragile | Tool existence is checked, not semantic support for the claim |
| Domain-boundary provenance hook | Wrapper/probe plus schema | Would scan proposed Edit/Write content against a repo manifest and require nearby markers | manifest `docs/tooling/domain-boundary-manifest.json`, settings route, ignored JSONL | implementation `scripts/domain_boundary_provenance_probe.py:1-30`, `:273-475`; schema exists; manifest absent; no settings route; global copies absent; last log 2026-09-06 | Disconnected | Prototype | Intentionally dormant, superseded, or undeployed? |
| Scrub gate | Pre-commit hook + GitHub Actions | Scans staged/all tracked text for selected private IP and secret shapes | Python stdlib, Git, allowlist | `.githooks/pre-commit:1-7`; local `core.hooksPath=.githooks`; `.github/workflows/scrub-gate.yml:1-17`; `scripts/scrub_gate.py:34-144`; all-tracked run passed | Implemented | Operational but fragile | No automated unit tests; scanner echoes matched values and ignores Git command failures |
| Hook telemetry | Python JSONL writer + aggregator | Best-effort local per-hook event logs and summaries | ignored local files; callers in three probes | writer `scripts/hook_telemetry.py:20-90`; aggregator paths `scripts/hook_telemetry_aggregate.py:23-44`; four observed logs | Partial | Operational but fragile | Domain hook uses a separate legacy shape; Postgres sink is a no-op |
| Postgres telemetry sink | Function stub, `scripts/hook_telemetry.py` | Declared future write to separate telemetry DB | none in v1 | `scripts/hook_telemetry.py:49-52` is `pass` | Proposed only | Concept | Requires a later decision; no implementation evidence |
| Session-start ground-truth scaffold | `reference/session_probe.py`, `reference/session-start-probe.sh` | Files copied by `/new-project` into other repositories | Git; deployment performed by `/new-project` | `commands/new-project.md:499-679`; reference probe `reference/session_probe.py:37-65` | Present — unverified as scaffold; disconnected locally | Prototype | Docstring says wired automatically, but Agent Rig settings wire session queue instead |
| Global Claude install surface | `~/.claude/{agents,commands,skills,templates,hooks,scripts}` | Runtime copies consumed outside this repository | manual/package installers and undeclared deployment steps | 79/84 package/source comparisons matched; four unique destinations drifted; hook/probe comparisons recorded in audit state | Partial | Operational but fragile | No manifest records origin/version/install time; root cross-cutting assets have no installer |
| Ignored runtime logs | Four JSONL files under `docs/tooling/` | Local audit trail for hook decisions | hook writers; aggregator for three logs | counts/hashes in `00-audit-state.md`; `.gitignore:11-32` | Implemented locally | Operational | Not clone-reproducible; retention/rotation not found |
| Claude transcript store | `~/.claude/projects/...` | Input for signpost validation and session staleness | Claude runtime; local filesystem conventions | `scripts/signpost_checklist_probe.py:316-340`; `scripts/session_queue_probe.py:173-219` | Present — unverified | Operational but fragile | Layout is assumed, not versioned by this repo |
| External LORE store | Postgres accessed read-only by session queue; write APIs named by commands | Cross-session memory and queue | external `.env`, psycopg2, MCP/apps | `scripts/session_queue_probe.py:66-149`; `commands/lore-close.md:52-98` | Present — unverified | Indeterminate | Excluded until reconciliation |
| Git/GitHub remote | `origin` | Published collaboration surface | SSH alias and GitHub | live `ls-remote`; CI workflow; main aligned | Implemented | Operational | Local stale remote-tracking refs and two ahead feature branches need separate interpretation |
| Archives and gate snapshots | `archive/`, committed `.gate-snapshots/` | Retained superseded bundles and review snapshots | none active found except documentation references | 119 archive files; numerous byte-identical duplicates; 14 ignored tooling-snapshot files plus committed snapshots | Duplicate/overlapping; mostly labeled archive | Indeterminate | Retention/currentness policy not established from implementation alone |

## Ownership evidence

Git history contains 193 commits authored as `dannySubsense` and 17 as `danny clarke`, both using the same email. README and ignored local instructions assign stewardship, but no machine-readable ownership file (`CODEOWNERS`, package manifest owners, or service unit ownership) was found. Ownership beyond commit attribution is therefore **Indeterminate**.

