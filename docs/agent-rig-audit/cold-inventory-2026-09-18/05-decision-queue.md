# Decision queue

Only questions repository and directly observable runtime evidence cannot settle are included. No target architecture is proposed.

| Decision required | Why evidence cannot settle it | Affected components | Evidenced options | Minimum resolver |
|---|---|---|---|---|
| Which copy is authoritative when committed source and `~/.claude` runtime differ? | Four installed destinations differ and no install manifest/provenance record identifies origin or intended version. | Frank, lore-close, Spec Intake/North-Star templates | HEAD source; current installed copy; another uncommitted/external source | Owner of runtime deployment plus direct diff/provenance record |
| Is the Notebook package's Frank intentionally allowed to share the global `frank.md` destination with cross-cutting Frank? | Both implementations target the same basename; observed runtime matches neither. | Notebook and all Frank-gated flows | Cross-cutting Frank; notebook-specific Frank; install-order-selected file | Agent Rig maintainer/author declaration after Phase 1 |
| Is domain-boundary provenance intentionally retired/dormant, or incompletely deployed? | Code/tests/log history exist, but settings route, manifest, and global install are absent. | Domain-boundary hook and claimed provenance controls | Dormant; local-only past experiment; deployment omission | Maintainer plus deployment record/current declaration |
| Is proof-less/manual-unverified completion an intended policy? | The hook name suggests proof-per-slice, while code explicitly allows no proof and nonallowlisted proof; Write is outside matcher scope. | Progress-proof hook, PROGRESS files, forge evidence claims | Optional evidence; mandatory only when allowlisted proof supplied; broader intended enforcement | Policy owner/author and governing decision record in Phase 2 |
| How does Claude Code combine user-level and project-level identical Stop hooks on this installed version, and is duplicate registration intended? | Both configurations are observed; no safe, nonmutating trace was available and docs/runtime behavior were not consulted externally. | No-preamble and signpost hooks, telemetry counts | Merge/fire twice; project override; runtime deduplication | Claude runtime documentation or a controlled post-freeze trace |
| Which Forge governance behavior governs: halt or synthesize missing INVARIANTS/CADENCE? | README/local instructions and command source conflict. | `/forge-start`, governance docs, downstream gates | README/CLAUDE halt; command's conditional authoring procedure | Maintainer or explicit canonical-source declaration |
| Are external user hooks (`check-spec-journaling.py`, `relay-hook.js`) Agent Rig-owned or merely adjacent? | They are configured in user settings but absent from this repository. | Effective Claude event pipeline | In scope as deployed Agent Rig controls; external shared platform dependencies | Runtime configuration owner/source repository |
| What is the supported deployment mechanism for cross-cutting root agents, commands, hooks, and probes? | Dated packages have installers; root surfaces do not, despite source-of-record claims. | Frank, new-project, lore-close, hook global copies | Manual copy; external deploy workflow; future repo mechanism | Maintainer plus deployment procedure evidence |
| Is LORE queue/schema availability current and compatible? | Phase 1 deliberately did not query LORE or inspect the external `.env`; code alone proves only expected SQL and paths. | Session queue, lore-close, relay/new-project memory steps | Current/compatible; unavailable; schema drift | LORE operator or Phase 2 read-only health/schema evidence |
| Which locally recorded remote-only refs and prunable worktrees should remain? | Their directories/remote branches are absent, but history/ongoing intent is not inferable from metadata alone. | Git maintenance and search noise | Retain as forensic metadata; remove later; recreate external state | Repository maintainer |
| Are dated active bundles releases, snapshots, or development sources with compatibility guarantees? | README calls them dated snapshots and evolving; no tags, releases, manifests, changelog contract, or compatibility metadata exist. | All four orchestration packages and consumers | Immutable snapshot; mutable development line; archive candidate | Package owner/release declaration |

Phase 2 sources such as the build-idea index, LORE, and author/orchestrator declaration may resolve these questions, but none were consulted before this queue was frozen.

