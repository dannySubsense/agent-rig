# Gap, overlap, and risk register

Priority expresses consequence supported by evidence, not a proposed redesign.

| Priority | Finding | Evidence plane and evidence | Status / consequence | Uncertainty |
|---|---|---|---|---|
| High | Progress-proof is not a mandatory proof gate. Settings invoke it only for `Edit`; the probe allows non-Edit operations, permits a completed slice with no `PROOF:`, and treats a nonallowlisted proof as `manual_unverified` without blocking. | HEAD: `.claude/settings.json:14-23`; `scripts/progress_proof_per_slice_probe.py:227-254`, `:524-568` | Partial. A `Write` or proof-less Edit can mark completion without executed evidence. | Intent cannot be resolved from implementation; the behavior is explicit. |
| High | Progress proof executes edit-supplied commands with `shell=True` after only string-prefix matching. Allowed prefixes include `pytest `, `python3 scripts/`, `bash tests/`, and `npm run `. | HEAD: `progress_proof_per_slice_probe.py:120-127`, `:205-215`; `docs/tooling/progress-proof-allowlist.json:1-11` | Implemented authority risk. Shell metacharacters after an allowed prefix are not rejected by the shown code. | No exploit was executed; consequence is inferred from Python shell semantics. |
| High | Scrub gate can disclose the secret-shaped value it detects and can report clean if its Git subprocess fails. | HEAD: token printed at `scripts/scrub_gate.py:136-142`; Git helper ignores return code/stderr at `:91-95`; empty files list passes at `:121-134` | Operational but fragile. A protective gate may leak into local/CI logs or fail open on Git failure. | Full scan passed in this environment; failure paths were not invoked. |
| High | Runtime “source of record” drift is demonstrated. Installed Frank, lore-close, INTAKE template, and NORTH-STAR template differ from committed sources; installed blobs were not found among current Git object listings. | Local runtime vs HEAD byte/hash comparison; source claims `agents/README.md:8-11`, `commands/README.md:8-11` | Partial provenance. Actual runtime behavior is not reproducible from HEAD for those surfaces. | Diff contents were intentionally not interpreted; direction/cause unknown. |
| High | Frank destination collision: root cross-cutting `agents/frank.md` and Notebook package `agents/frank.md` both install to `~/.claude/agents/frank.md`; neither matches the observed installed file. | HEAD installers copy basenames (`notebook.../install.sh:44-49`); both source paths exist; local comparisons | Duplicate/overlapping. Install order can change the active judgment persona. | No install was run; current installed origin is indeterminate. |
| Medium | Progress wrapper's isolated failure telemetry is broken after the shared telemetry dependency was introduced. | Test evidence: `test_progress_proof_per_slice_wrapper.sh` 9 pass / 6 fail; `ModuleNotFoundError: hook_telemetry`; wrapper imports target-repo `scripts` at `.claude/hooks/progress-proof-per-slice.sh:40-75` | Partial. Wrapper still fails open, but the promised error record is missing in isolated deployment failure paths. | Active Agent Rig repo contains the module, so this specifically proves portability/error-fixture failure. |
| Medium | Domain-boundary implementation has no active route in Agent Rig. | HEAD settings omit it; required manifest named at `domain_boundary_provenance_probe.py:40-43` is absent; global wrapper/probe absent; local log last event 2026-09-06 | Disconnected. Current edits are not checked by this mechanism. | It may be intentionally dormant; documentation intent was not used to decide. |
| Medium | No-preamble is described as enforcement, but current probe mode is `log_only`; flagged clauses allow completion. | HEAD `scripts/no_preamble_probe.py:49-51`, `:417-425`; logs contain 61 `flagged` decisions | Partial blocking capability; advisory behavior is implemented. | One historical `block` row predates/currently contradicts the current mode but was not interpreted. |
| Medium | Signpost “verified” status establishes only that a completed, nonexcluded tool-call ID exists; it does not bind the call input/result to the claim text. | HEAD `signpost_checklist_probe.py:444-480`, `:514-533` | Partial provenance. A real but unrelated tool call can satisfy the mechanical check. | Semantic review may be supplied by the model/human, but not by this probe. |
| Medium | Hook controls systematically fail open and local telemetry is best-effort. | Wrapper headers/branches; `hook_telemetry.py:78-90`; domain main `:453-475`; progress main `:597-619`; signpost main `:897-913` | Availability failures permit the guarded action and may also lose evidence. This is implemented policy, not a latent bug claim. | Operational failure rate observed only for logged hooks. |
| Medium | Telemetry is fragmented and local-only. Aggregator lists three hooks, excludes domain; domain log uses legacy fields; database sink is empty; logs are gitignored. | HEAD `hook_telemetry_aggregate.py:23-44`; `hook_telemetry.py:49-52`; `.gitignore:11-32`; local runtime counts | Partial. Clone/remote cannot reproduce the audit trail; combined count omits 169 domain rows. | Retention/centralization intent is unresolved. |
| Medium | Orchestration gate authority is prompt-based. “Binding” sequences are Markdown interpreted by an external runtime; no executable controller prevents direct agent invocation or skipped steps. | HEAD command flows `spec-start.md:495-632`, `forge-start.md:336-471`; agent definitions grant Write/Bash as applicable; no runtime package/daemon manifest | Present — unverified. Operational compliance depends on Claude/human behavior. | This does not prove bypass occurred; it limits what the repository itself demonstrates. |
| Medium | CI verifies only the scrub gate, not the 208 passing Python tests or wrapper scripts. | HEAD `.github/workflows/scrub-gate.yml:1-17`; no other workflow files or test commands found | Partial quality gate. Regressions can merge without repository-configured test execution. | External CI not represented in the repo could exist; none was evidenced. |
| Medium | Notebook bundle is installed but cannot execute on this host because `jupyter` is absent. | Runtime `command -v jupyter` absent; requirement `notebook-execute/SKILL.md:38-49` | Partial/local-only. Design/build prompts exist; execution path is blocked here. | Another host may satisfy it. |
| Medium | Runtime dependencies are implicit and unpinned. No requirements, pyproject, package manifest, or lockfile was tracked. | Targeted `git ls-files` search; imports include psycopg2 (`session_queue_probe.py:100-131`) | Operational but fragile. Reproduction depends on host-global packages/tools. | Current host has many dependencies; other hosts unverified. |
| Medium | Documentation and implementation disagree on Forge governance behavior. README and ignored local instructions say missing INVARIANTS/CADENCE halt; current command authors missing files when source material exists. | HEAD `forge-07152026/README.md:22-32`; command `forge-start.md:13-31`; ignored `CLAUDE.md:142-165` | Documentation drift. Different readers can follow different gates. | Command is the closest executable prompt surface, but formal authority was not adjudicated. |
| Low | `commands/README.md` lists only `new-project` as resident although tracked `lore-close.md` is also present; `agents/README.md:4-6` names old package directories now under archive. | HEAD path inventory and cited lines | Documentation drift. | No runtime consequence established. |
| Low | Archive/snapshot duplication is substantial. 119 files are under `archive/`; many active agent/skill/template blobs are byte-identical to two archived versions, and committed gate snapshots duplicate live docs/config. | HEAD object-ID grouping, e.g. three identical Forge agent copies and three identical settings files | Duplicate/overlapping. Search and ownership surfaces are noisy, though most paths are labeled archive/snapshot. | Not declared obsolete unless path/history supports it. |
| Low | Git metadata retains three prunable worktrees and three stale remote-tracking refs absent from live remote. | Git metadata plane: `git worktree list --porcelain`, local refs vs `git ls-remote` | Obsolete/abandoned candidates. No active directory exists at registered paths. | No prune/fetch was performed, so cleanup intent is unknown. |
| Low | Repository has no declared license. | HEAD: no `LICENSE`; `README.md:79-83` states none | Documentation-confirmed legal limitation for reuse. | Legal interpretation beyond file presence is outside scope. |

## Not found / indeterminate searches

- No repo-owned daemon/service unit, API server, web dashboard, TUI, Docker definition, or MCP server implementation was found in the tracked tree.
- No submodule, nested repository, or symlink was found.
- No package/dependency manifest or lockfile was found.
- No active caller was found for the domain-boundary wrapper.
- No automated tests were found for end-to-end Spec, Forge, Notebook, Lit Synthesis, `/new-project`, or `/lore-close` runtime behavior.

These statements describe the searches performed; they do not claim universal absence outside the repository.

