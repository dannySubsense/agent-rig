# Capability matrix

| Capability | Implementation | Active caller / consumer | Evidence | Status | Maturity | Known limitations |
|---|---|---|---|---|---|---|
| Distribute dated Claude prompt bundles | Four `install.sh` scripts copy agents, commands, skills, templates with backups | Global `~/.claude` runtime | Forge installer `:16-95`; Spec installer `:13-88`; observed installed-byte matches | Implemented | Operational but fragile | Mutating installers not exercised; no uninstall/version manifest; four runtime destinations drift |
| Produce a full product spec | `/spec-start` Markdown orchestrator plus 6 role/skill pairs and templates | Claude slash-command runtime | `spec-orchestration-07152026/commands/spec-start.md:51-63`, `:201-218`, `:306-489` | Present — unverified | Operational but fragile | Enforcement is prompt/runtime behavior; no end-to-end test or trace inspected |
| Produce a bounded tooling spec | `/spec-start --lite` | Claude runtime | `spec-start.md:70-180` | Present — unverified | Prototype | Scope classification is textual judgment |
| Implement a spec through separated roles | `/forge-start`, 7 agents/skills, progress file contracts | Claude slash-command runtime | `forge-start.md:193-274`, `:475-510` | Present — unverified | Operational but fragile | Agents and gates can be bypassed outside the command; no executable controller |
| Binding Frank spec/forge review | Frank persona + command retry/snapshot instructions | `/spec-start`, `/forge-start`, notebook command | Frank metadata `agents/frank.md:1-15`; spec gate `spec-start.md:495-632`; forge gate `forge-start.md:336-471` | Present — unverified | Operational but fragile | Runtime Frank differs from HEAD; “binding” is instruction-layer authority, not a repository service |
| Notebook design/build/execute/QC | Notebook command, agents, skills, templates | Claude runtime + Jupyter | `notebook-start.md:40-120`; `notebook-execute/SKILL.md:29-49` | Partial | Prototype | `jupyter` absent locally; notebook Frank collides with cross-cutting Frank |
| Literature corpus synthesis | Three commands, five roles/skills, templates | Claude runtime over user corpus | `lit-synthesis-run.md:44-106`, `:157-227`; installed copies matched | Present — unverified | Operational but fragile | No package-specific automated tests or observed live run in scope |
| Bootstrap a new repository | `/new-project` procedural command and reference scaffold | Global Claude command | `commands/new-project.md:5-31`, `:360-461`, `:499-679`, `:813-855` | Present — unverified | Operational but fragile | Necessarily mutates files/Git/GitHub/LORE; not exercised |
| Close a session and hand off state | `/lore-close` | Global Claude command, session queue reader | `commands/lore-close.md:52-98`, `:111-137`; queue tag reader `session_queue_probe.py:95-149` | Partial | Operational but fragile | Installed command differs; external LORE excluded |
| Inject prior-session queue on startup | SessionStart wrapper/probe and SQL | Repo-local `.claude/settings.json` | settings `:2-12`; read-only SQL `session_queue_probe.py:95-149`; output `:267-407` | Present — unverified | Operational but fragile | Local-only `.env`, external DB, and transcript layout dependencies |
| Require proof before marking a slice complete | Progress proof PreToolUse hook | Repo-local settings matcher `Edit` | settings `:14-23`; probe `:524-594` | Partial | Operational but fragile | `Write` bypasses hook; no `PROOF:` allows completion; nonallowlisted proof is `manual_unverified` and allows; shell prefix matching is weak; wrapper test failures |
| Enforce no-preamble response style | Reminder + analyzer + Stop wrapper | Repo and global settings | reminder `no_preamble_reminder.py:17-42`; mode `no_preamble_probe.py:49-51`, decisions `:399-425` | Partial | Operational | Current mode logs flags but does not block; global and local duplicate registration unresolved |
| First-turn claim/evidence checklist | Signpost Stop hook | Only first turn after successful queue marker | `signpost_checklist_probe.py:405-480`, `:828-894`; 238 runtime rows; tests pass | Implemented | Operational but fragile | A cited tool ID only has to exist; semantic support is not evaluated (`:514-533`) |
| Domain-boundary citation checking | Domain probe/wrapper/schema | No active caller found | `.claude/settings.json` has no route; manifest absent; probe `domain_boundary_provenance_probe.py:273-475` | Disconnected | Prototype | Fail-open if manifest absent/invalid; global copies absent |
| Scrub selected secrets/private addresses before publish | Shared Python scanner, local pre-commit, CI | Git commit and GitHub push/PR | `scrub_gate.py:34-144`; `.githooks/pre-commit`; CI workflow; manual all-tracked pass | Implemented | Operational but fragile | Selected patterns only; matched token echoed; Git subprocess failures ignored; CI runs no tests beyond scrub |
| Normalize hook telemetry | Shared writer and aggregator | Three probes; ignored logs | `hook_telemetry.py:20-90`; aggregator `:23-44`; 2,115 aggregated rows | Partial | Operational but fragile | Domain log excluded; database sink no-op; logs ignored/local only |
| Persist telemetry to database | `write_telemetry_pg()` placeholder | Called after local write | `hook_telemetry.py:49-52`, `:87-90` | Proposed only | Concept | No connection or write |
| Recover/resume orchestration | PROGRESS files and command instructions | Spec, forge, notebook, lit commands | `forge-start.md:574-633`; `lit-synthesis-run.md:44-106`; `notebook-start.md:238-282` | Present — unverified | Operational but fragile | State is manually interpreted Markdown; no state machine or schema validation found |
| Automated code verification | Python tests for probes/wrappers | Manual pytest; no CI test job | 208 passed/1 excluded; domain wrapper 15/15; progress wrapper 9/15; CI file only runs scrub | Partial | Operational but fragile | Orchestration bundles lack direct tests; one wrapper suite fails |
| Git branch/PR workflow | Prompt instructions and GitHub Ops agent | `/forge-start`, `/new-project`, `/lore-close` | `forge-start.md:35-99`; `github-ops.md:19-36`; live remote capture | Present — unverified | Operational | No command invoked in audit; two feature branches ahead locally |
| Local runtime supervision / service recovery | No repo-owned service/unit definition found | None | targeted manifest search found no Docker/service/package definitions; SessionStart/Stop hooks are event-driven, not supervisors | Not found | Indeterminate | Search covered tracked tree and user service metadata; absence is not universal proof |
| Web/dashboard/TUI/MCP server | No implementation entry point found | None | tracked tree has one static HTML diagram and Markdown commands; no server/package manifest | Not found | Indeterminate | External LORE/Switchboard apps are dependencies, not implemented here |

## Test evidence boundary

The passing test result verifies probe functions and selected wrapper behavior, not the Claude runtime's interpretation of Markdown commands, global installation semantics, external LORE, or human/Frank judgment. The progress-wrapper failures are direct contradictory evidence against treating all wrapper error paths as verified.

