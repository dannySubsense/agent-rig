# Fragment-Shaped Recall — Committed Results

**Generated by**: `docs/research/domain-boundary-hook-benchmark/scan_fragments.py`  
**Regenerate**: `python3 docs/research/domain-boundary-hook-benchmark/scan_fragments.py`  
**Python**: 3.12.3  
**Detector under test**: `scan_source_fragment` in `scan_thresholds.py` — the three-strategy
chain specified by `02-ARCHITECTURE.md` §2.1 (ast.parse → dedent-retry on IndentationError →
regex fallback for the assignment context only).

This file is machine-generated. Do not hand-edit — rerun the script.

## 1. Why this run exists

`results.md` §4's 2/2 recall was measured against **whole `.py` files**. The hook's real scan
surface is `tool_input.new_string` / `tool_input.content` — a **fragment**. `02-ARCHITECTURE.md`
§2.1 records the whole-file figure as *"NOT YET VALIDATED AT THE REAL SCAN SURFACE"* and
specifies this exact re-run as its required follow-up. This is that run.

## 2. Fragment test matrix

Every fragment below is sliced live from real source at runtime (I1 from the working tree,
I2 from `git show 7d9fdf5:research/pipeline/config.py`) — none is hand-typed.

| Case | Incident | Fragment shape | Strategy that ran | Result |
|---|---|---|---|---|
| `I1-a` | I1 | changed line only (module level, column 0) | 1 direct ast.parse | **PASS** |
| `I1-b` | I1 | changed line + 2 lines of real preceding context (comment block) | 1 direct ast.parse | **PASS** |
| `I1-c` | I1 | changed line + 3 lines before and after (real surrounding code) | 1 direct ast.parse | **PASS** |
| `I2-a` | I2 | changed line only, ORIGINAL 4-space indent, NO enclosing class (worst case) | 2 dedent-retry | **PASS** |
| `I2-b` | I2 | changed line + 2 lines of real preceding context (indented, no class line) | 2 dedent-retry | **PASS** |
| `I2-c` | I2 | changed line + enclosing `class ...:` line + other body lines | 1 direct ast.parse | **PASS** |
| `I2-d` | I2 | changed line + 3 real body lines after (indented, no class line) | 2 dedent-retry | **PASS** |
| `I2-e` | I2 | fragment OPENS with a dangling `])` closing a list begun above the window (unbalanced) | 3 regex fallback | **PASS** |
| `I2-f` | I2 | changed line at 4-space indent preceded by a column-0 line (dedent finds no common prefix) | 3 regex fallback | **PASS** |
| `F1` | F1 (float probe, not ground truth) | FLOAT threshold `dilution_pct_min: float = 0.10`, forced onto regex path (col-0 line first) | none (no candidates) | **MISS** |

### Per-case detail

**`I1-a` — PASS** (changed line only (module level, column 0))

```python
_HEAD_BYTES = 65_536
```

- Strategy: `1 direct ast.parse`
- Total candidates from fragment: 1
- Target hits: line 1 ctx=assign_module value=65536 net_flagged=True

**`I1-b` — PASS** (changed line + 2 lines of real preceding context (comment block))

```python
# read size, not a claim about header length. The script REPORTS any file whose header fields
# were not found within it rather than silently treating them as missing.
_HEAD_BYTES = 65_536
```

- Strategy: `1 direct ast.parse`
- Total candidates from fragment: 1
- Target hits: line 3 ctx=assign_module value=65536 net_flagged=True

**`I1-c` — PASS** (changed line + 3 lines before and after (real surrounding code))

```python
# Header scan window. The SEC header sits at the top of every submission; 65,536 bytes is the
# read size, not a claim about header length. The script REPORTS any file whose header fields
# were not found within it rather than silently treating them as missing.
_HEAD_BYTES = 65_536

_RE_FORM = re.compile(r"^CONFORMED SUBMISSION TYPE:\s*(\S+)", re.M)
_RE_CIK = re.compile(r"^\s*CENTRAL INDEX KEY:\s*(\d+)", re.M)
```

- Strategy: `1 direct ast.parse`
- Total candidates from fragment: 1
- Target hits: line 4 ctx=assign_module value=65536 net_flagged=True

**`I2-a` — PASS** (changed line only, ORIGINAL 4-space indent, NO enclosing class (worst case))

```python
    filing_text_max_bytes: int = 512_000
```

- Strategy: `2 dedent-retry`
- Total candidates from fragment: 1
- Target hits: line 1 ctx=assign_module value=512000 net_flagged=True

**`I2-b` — PASS** (changed line + 2 lines of real preceding context (indented, no class line))

```python

    # Pipeline settings
    filing_text_max_bytes: int = 512_000
```

- Strategy: `2 dedent-retry`
- Total candidates from fragment: 1
- Target hits: line 3 ctx=assign_module value=512000 net_flagged=True

**`I2-c` — PASS** (changed line + enclosing `class ...:` line + other body lines)

```python
class BacktestConfig:

    # Pipeline settings
    filing_text_max_bytes: int = 512_000
    fetch_concurrency: int = 8
    fetch_rate_limit_per_sec: int = 10
    fetch_timeout_sec: int = 30
```

- Strategy: `1 direct ast.parse`
- Total candidates from fragment: 4
- Target hits: line 4 ctx=assign_class value=512000 net_flagged=True

**`I2-d` — PASS** (changed line + 3 real body lines after (indented, no class line))

```python
    filing_text_max_bytes: int = 512_000
    fetch_concurrency: int = 8
    fetch_rate_limit_per_sec: int = 10
    fetch_timeout_sec: int = 30
```

- Strategy: `2 dedent-retry`
- Total candidates from fragment: 4
- Target hits: line 1 ctx=assign_module value=512000 net_flagged=True

**`I2-e` — PASS** (fragment OPENS with a dangling `])` closing a list begun above the window (unbalanced))

```python
    ])

    # Filter thresholds (must match live settings and 01-REQUIREMENTS.md filter table)
    market_cap_max: int = 2_000_000_000    # $2B (Filter 2: market_cap < $2B)
    float_max: int = 50_000_000            # 50M shares (Filter 3: float < 50M)
    dilution_pct_min: float = 0.10         # 10% (Filter 4: dilution > 10%)
    price_min: float = 1.00                # $1.00 (Filter 5: price > $1.00)
    adv_min: float = 500_000               # $500K dollar volume (Filter 6: ADV > $500K)

    # Pipeline settings
    filing_text_max_bytes: int = 512_000
```

- Strategy: `3 regex fallback`
- Total candidates from fragment: 4
- Target hits: line 11 ctx=assign_module value=512000 net_flagged=True

**`I2-f` — PASS** (changed line at 4-space indent preceded by a column-0 line (dedent finds no common prefix))

```python
# Pipeline settings
    filing_text_max_bytes: int = 512_000
```

- Strategy: `3 regex fallback`
- Total candidates from fragment: 1
- Target hits: line 2 ctx=assign_module value=512000 net_flagged=True

**`F1` — MISS** (FLOAT threshold `dilution_pct_min: float = 0.10`, forced onto regex path (col-0 line first))

```python
# Filter thresholds
    dilution_pct_min: float = 0.10         # 10% (Filter 4: dilution > 10%)
```

- Strategy: `none (no candidates)`
- Total candidates from fragment: 0
- Target hits: target not detected

## 3. Recall by incident

| Incident | Cases | PASS | MISS | Recall |
|---|---|---|---|---|
| F1 (float probe, not ground truth) | 1 | 0 | 1 | 0/1 |
| I1 | 3 | 3 | 0 | 3/3 |
| I2 | 6 | 6 | 0 | 6/6 |
| **GROUND TRUTH (I1+I2 only)** | 9 | 9 | 0 | **9/9** |

`F1` is a deliberate limitation probe, not a historical incident — it is excluded from the
ground-truth recall figure and reported separately in §5.

## 4. Strategy attribution

| Strategy | Cases where it was the one that ran |
|---|---|
| 1 direct ast.parse | `I1-a`, `I1-b`, `I1-c`, `I2-c` |
| 2 dedent-retry | `I2-a`, `I2-b`, `I2-d` |
| 3 regex fallback | `I2-e`, `I2-f` |
| none (no candidates) | `F1` |

## 5. What this run does NOT establish

- **Contexts 1 and 2 (comparison operand, slice/truncation) are not measured here.** §2.1
  gives them no regex fallback by design; an unparsable fragment yields zero candidates from
  them. This run measures context 3 only, because both ground-truth incidents are context 3.
- **The regex fallback matches integers and booleans only** —
  `(-?\d[\d_]*|True|False)` per §2.1. A float threshold (e.g. `dilution_pct_min: float = 0.10`,
  a real line four rows above I2 in the same dataclass) does **not** match it, so a float
  threshold in an unparsable fragment is a known MISS. Neither ground-truth incident is a
  float, so this does not affect the recall figures above — it is recorded, not hidden.
- **Precision is not measured.** No false-positive labeling was done on the regex fallback.
- The `{0, 1, -1, 2}` exclusion set is applied identically to the ast and regex paths and
  remains unvalidated (`results.md` §6).

